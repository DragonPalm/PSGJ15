extends Node

var map_stage = 0
var level_name = null
var unlocked_maps = ["crypt"]

func tasks_to_complete():
	# Make logic to change the level scene based on which level the player is going to
	# Lock out world map options from non-chosen paths
	# Create a seed so that random generation logic is always the same in the same playthrough
	# Combat logic
	# Pause menu, savegame feature, options menu
	pass
